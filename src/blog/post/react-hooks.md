# Understanding React Hooks

React Hooks allow you to use state and lifecycle features in functional components.

## useState

The useState hook lets you add state to your component.

Example:

```js
const [count, setCount] = useState(0)